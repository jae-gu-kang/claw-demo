# 컨텍스트 문서 2/2 — 구현 (툴·소프트웨어)

> 비행제어/유도법칙 설계툴 프로젝트의 **소프트웨어 구현** 컨텍스트.
> 도메인·설계 결정은 `fcs-context-01-control-law.md` 참조.
> 상태 표기: **[확정]** 결정 완료 / **[기본값]** 잠정 채택, 변경 가능 / **[TBD]** 미결

---

## 1. 스코프

**[확정]** 범위는 **설계 · 해석 · 시뮬레이션**까지.
- 포함: 6DOF 플랜트, 트림, 선형화, 안정성 해석, 제어법칙 설계·실행, 비선형 시뮬레이션, 검증·리포트
- **[확정]** **탑재 제어법칙 C 생성은 범위 안**이다 (v0.20에서 확대 — 종전 "FCC 탑재용 비행코드 생성 제외, 별도 모듈로 추후 부착"을 대체). 전제: 생성 코드는 **FCC에 통합되어 그대로 실린다**. FCC팀이 신뢰성 시험을 거쳐 일부 수정할 수는 있으나 MATLAB 코드젠 산출물처럼 거의 그대로 탑재된다
  - 여전히 범위 밖: **FCC 전체** — 주기 스케줄러, I/O(ARINC/1553/CAN), 입력 유효성·신선도, 다중화·투표, BIT, 페일세이프 리버전, 모드 상태기계. 우리가 내는 것은 제어법칙 한 덩이다
  - **[확정]** 구조의 정본은 **IR 하나**이고, Python 실행과 C 생성은 그 **백엔드**다(§2.2, M16). 손으로 쓴 C를 따로 두지 않는다 — 제어법칙을 두 벌로 가지면 §5.5 중복 정의 금지를 정면으로 어기고, 대조 테스트는 어긋남을 발견할 뿐 막지 못한다
  - **[확정] 한계** — 소스가 같아도 타깃 컴파일러·최적화 옵션·부동소수 폭 차이는 남는다. 측정 사례: FMA 축약(`-ffp-contract=fast`)만 켜도 곱셈-덧셈이 합쳐져 비트 일치가 깨진다(최대 2.8e-16, clang 14 -O2). 생성 헤더가 빌드 요구를 명시하되, **타깃에서의 확인(PIL 성격)은 FCC팀 몫**이다. 과대 주장하지 않는다
  - 웹 코드 패널(§4)은 **두 가지를 나란히** 낸다: ① 설계 형상의 코드 표현(엔진 Python 재현 스크립트 + C 파라미터 헤더, v0.17) — 탑재물이 아니라 검토·산출물용 ② **탑재 C**(v0.24) — 위의 생성물 그 자체다. 웹은 C를 조립하지 않고 엔진 생성기(`POST /codegen/flight`)를 부르며, 같은 형상이면 커밋된 산출물 `flight/gen/`과 바이트 단위로 같다. 두 탭이 내는 물건이 다르므로 화면 안내 문구도 탭마다 다르다
- **[확정]** 개방망 우선 개발, 폐쇄망 적용 추후 검토 → **의존성 최소화** 설계 (프론트엔드 JS 포함)

## 2. 아키텍처

### 2.1 MATLAB/Simulink 기능 대체 매핑

**[확정]** 본 프로젝트의 실질 목표는 **MATLAB 없는 제어법칙 설계**다. 아래는 현행 워크플로우에서 실제 사용하는 블록·툴박스 조사 결과와 대체 경로.

| MATLAB/Simulink 기능 | 대체 방안 | 상태 |
|---|---|---|
| `trim` | scipy.optimize (SLSQP / trust-constr) | 확정 |
| `linmod` / `linearize` | 수치섭동 선형화 (자체 구현) | 확정 |
| Control System Toolbox — `margin`, `damp`, `eig`, `bode`, Root Locus | python-control | 확정 |
| Control System Toolbox — PID, State Space, Transfer Function, Discrete Filter, Zero-Pole | 제어 요소 라이브러리 (§2.2) | 확정 |
| Control System Toolbox — LQR 설계 | **스코프 제외** (제어법칙은 PI 기반, 도메인 §3) | 확정 |
| Simulink 6DOF 시뮬레이션 | C++ 코어 | 확정 |
| 블록 다이어그램 설계 | 고정 아키텍처 + 파라미터/옵션 (웹 UI) | 확정 |
| Aerospace Blockset — 6DOF, Quaternion, DCM, Coordinate Transform | 자체 구현 (C++ 코어) | 확정 |
| Aerospace Blockset — Atmosphere, Gravity, Earth Model(WGS-84), Wind Model | 자체 구현 (Wind는 확장 항목) | 확정 |
| Lookup Table / n-D Lookup 블록 | 자체 보간 엔진 (C++) | 확정 |
| MATLAB Function 블록 (공력·유도 커스텀 로직) | C++ / Python 코드로 직접 구현 | 확정 |
| Signal Processing — LPF, HPF, Moving Average, FIR, IIR | 제어 요소 라이브러리 (§2.2) | 확정 |
| Stateflow (모드 전환) | **불필요** — 선언적 모드 테이블 + 실행기 | 확정 |
| Monte Carlo (`sim` 병렬) | Python 배치 실행 (멀티프로세스) | 기본값 |
| **Simulink Data Dictionary** (Kp, Ki, WingArea, Mass, Ix/Iy/Iz, CG 등 변수관리) | **파라미터 관리 계층** (§4.5) | 확정 — 신규 |
| Kalman Filter / EKF (항법) | **미구현** — 등가 오차 모델로 대체 (§3.1), 인터페이스 개방 | 확정 |
| Optimization Toolbox — 자동 PID 튜닝 | **스코프 편입** (v0.30 번복 — M17 자동 설계 루프): 결정론 루프쉐이핑(레이트 댐퍼 모드 감쇠 목표 + 자세 PI 목표 교차 백오프, 작동기·지연 항상 포함) + 제한 Nelder-Mead 마무리(전역 polish는 기본 OFF 유지 — 결정론·재현성 우선. 단 **대역폭이 무너진 실패 자리에 한해서는 구제 마무리를 항상 시도**하고, 백오프 루프와 같은 수용식 + 대역폭 하한을 다시 통과할 때만 채택한다 — 결과를 나쁘게 만들 수 없는 경로다. 01 v0.26). 사람 승인 게이트(gated)가 기본이고 상위 설계 변경 에스컬레이션은 자동 적용 금지. LQR 제외는 유지(PI 구조 불변 — 튜닝 방식만 자동화) | 확정 — 번복 |
| Optimization Toolbox — Parameter Estimation | **스코프 제외** (수동 튜닝으로 설계, 추후 확장 여지) | 확정 |

- **[TBD]** 위 표에 없는 현행 기능 추가 발굴 — 프로젝트 리스크 관리 항목 (현행 모델 열람 가능 시점에 재조사)

### 2.2 제어 요소 라이브러리 (Primitive Block Set)

**[확정]** 아래 원시 요소들이 C++ 코어의 구현 범위이자, 추후 코드젠 모듈의 생성 단위가 된다. 각 요소는 레지스트리+스키마 원칙(§2.3)을 따라 웹 UI에서 자동 편집 가능해야 한다.

| 분류 | 요소 |
|---|---|
| 기본 연산 | Gain, Sum, Product, Divide, Switch/Selector, Unit Delay, Memory |
| 적분·미분 | Integrator (안티와인드업 포함, 초기화 가능), Derivative |
| 선형 필터 | 1차 지연(Lag), Lead-Lag, Washout(HPF), Low-Pass, Notch, Discrete Filter, Zero-Pole, FIR/IIR, Moving Average |
| 제어기 | P / PI / PID (게인 스케줄 가능), State-Space, Transfer Function |
| 비선형 | Saturation, Rate Limiter, Dead Zone, **Backlash**, Hysteresis |
| 테이블 | 1D/2D/nD Lookup Table (보간·외삽 옵션) |
| 항공 전용 | Quaternion 연산, DCM, 좌표변환, 삼각함수 유틸 |
| 신호 관리 | Delay, Fader (범프리스 전환용), 상태 초기화 인터페이스 |

- Simulink 사용 실적상 기본 블록(Integrator, Sum, Gain, Product, Saturation, Switch, Rate Limiter, Lookup Table, Unit Delay, Memory)이 전체의 절반 이상을 차지 → 이 집합의 구현 품질·검증이 최우선
- **[확정]** 이 집합이 **탑재 C 생성의 단위**다 (M16 codegen). 제어법칙 구조의 정본은 IR이고 블록 구현은 백엔드다 — 블록의 Python `step()`과 C 에미터는 같은 명세의 두 구현이므로 **블록별 등가성 대조로 못박는다**. 이건 §5.5 중복 정의 금지의 예외가 아니라 성격이 다른 것이다: 블록 집합은 **고정·유한(25종)** 이라 대조로 관리되지만, 제어법칙 구조는 계속 바뀌므로 반드시 단일 정본이어야 한다 (MathWorks도 시뮬 엔진과 TLC 템플릿으로 블록을 이중 구현하고 SIL/PIL로 대조한다)
- **[확정]** **이산 계수는 생성 시점에 구워** 파라미터 데이터로 낸다 — 탑재에서 `exp()` 등 libm 호출을 없애 결정론을 확보한다. 계수는 엔진이 `init(dt)`에서 계산한 값을 인스턴스에서 그대로 읽어 굽는다(이산화 공식이 두 번 적히지 않는다). 부작용: **주기(dt)가 형상의 일부**가 되어 지문에 포함되고, 생성 코드에서 dt는 튜닝 가능한 파라미터가 아니라 매크로다 — 값만 고치면 필터 계수가 조용히 틀리기 때문
- **[확정]** 생성은 **결정적**이다(생성 시각 미포함). 산출물을 커밋하므로 "재생성했더니 달라졌다"가 곧 실제 설계 변경이어야 한다
- **[확정]** **모드 분기는 enable 영역으로 표현한다** — 제어법칙에는 "축이 비활성이면 명령필터가 측정을 추적하고 헤딩 적분기를 소거한다"(autopilot.py:147) 같은 상태 부작용이 있어 순수 데이터흐름으로는 표현되지 않는다. Simulink의 Enabled Subsystem과 같은 의미론을 노드 단위로 두고, 같은 enable을 가진 **연속** 노드가 한 영역이 되어 생성 C의 `if/else` 한 덩이가 된다. 최상위의 항법 무효 홀드(law.py:86)는 **그래프 전체의 enable** — 아무것도 실행하지 않고 직전 출력을 그대로 내며 상태도 동결된다
- **[확정]** **제어법칙 구조의 정본은 IR 하나다** (v0.22 이관 완료). `claw/fcl/graphs.py`의 그래프 선언이 구조 그 자체이고, `Autopilot`·`Scas`·`Mixer` 등 클래스는 파라미터를 보유하고 그 그래프를 태우며 원시 항법 상태를 공학량으로 바꿔 주는 **어댑터**다. 설계 실행(Python 백엔드)과 탑재 C(C 백엔드)가 같은 선언에서 나오므로 어긋날 수 없고, "Python과 C를 둘 다 운용할지"는 나중에 정해도 되는 **백엔드 선택**으로 남는다
  - 부수 규칙: 조립된 뒤 **상태는 조립의 러너 한 곳에만** 있다. 자식 인스턴스에 웜스타트를 넣으면 법칙 실행에 반영되지 않으므로 조립에 넣는다(`Scas.reset(states=…)`). 실제로 이걸 어겨 트림 웜스타트가 사라진 적이 있고, 지금은 그 경로가 조용히 무시되는 대신 예외로 터진다
  - 비용은 쟀다: 데모 미션 180 s 폐루프가 11.08 s → 11.37 s (**+2.6%**). 6DOF·공력이 지배적이라 IR 순회 오버헤드는 묻힌다
- **[확정]** **생성 C는 두 축으로 나눈다** — 역할축(`.c` 알고리즘 / `_data.c` 파라미터=rtP / `_types.h` 자료형·dt 매크로 / `.h` 진입점)과 **기능축**(서브시스템별 `{base}_{group}.c`·`.h`). MATLAB의 `File packaging format: Modular` + 서브시스템 `Function packaging: Nonreusable function` · `File name options: Use subsystem name`에 해당한다. 목적은 FCC팀이 **서브시스템 하나만 떼어 읽고 검증**할 수 있게 하는 것이다. 분할 단위는 IR 노드의 이름표(`grouped`)이고 **IR은 평탄한 채로 남는다** — 계층화하면 호출 규약이 IR로 올라와 선언순서·도달성·enable 검사가 전부 계층을 인지해야 하고, "선언 순서 = 실행 순서 = 생성 C 문장 순서"라는 성질을 잃는다. Python 실행기는 이름표를 **아예 읽지 않으므로** 분할이 실행 결과를 바꿀 수 없다
  - 경계를 넘는 신호는 함수 인자가 되며 **이름이 그대로 유지된다** — 정의부 지역변수도, 인자도, 호출부 지역변수도 `ap_theta_out_y`다 (`rtb_Sum_p_idx_1` 류를 만들지 않는다는 요구가 경계에서도 지켜져야 한다)
  - 파라미터·상태 구조체는 **쪼개지 않는다**. 파티션 함수들이 `{base}_params_t`·`{base}_state_t` 포인터를 함께 받으므로 `{base}_reset`과 범프리스 웜스타트 계약이 그대로 남는다
  - enable 영역이 group 경계를 가로지르면 IR이 **거부한다** — `if` 한 덩이가 두 함수에 걸칠 수 없다. 이름표는 연속이어야 하고, 붙일 거면 전부 붙여야 한다(일부만 쪼갠 코드는 "나머지는 어디 있나"를 읽는 사람에게 떠넘긴다)
- **[확정]** **형상 지문은 파일 배치와 무관하다** — 지문은 파라미터·dt·노드 구조의 신원이므로, 어떻게 쪼개든 같은 법칙이면 같은 지문이다(분할 전후 `c0f9af6f848059c4` 유지 확인). 배치 변경은 파일 diff로 보이면 되고, 지문까지 흔들리면 "형상이 바뀐 것인가"를 구별할 수 없게 된다
- **[확정]** **공용 헬퍼는 산출물마다 복제하지 않고 `claw_rt.c`·`.h` 한 벌로** 낸다 (MATLAB `slprj/ert/_sharedutils/` 대응) — 같은 코드를 산출물 수만큼 검증하지 않기 위해서다. 쓰는 것만 내고(안 쓰는 헬퍼는 탑재 코드의 dead code다), 산출물이 여럿이면 **헬퍼 합집합으로 한 번** 만든다. 산출물별로 따로 내면 헬퍼가 적은 쪽이 덮어써 링크가 조용히 깨진다
- **[확정]** **생성 제어법칙의 입력 경계는 "이미 계산된 공학량"** (θ·φ·ψ·p·q·r·V·α·β·h·ḣ·mach)이다. 실기 FCC에서 오일러각·에어데이터·마하는 항법·ADC가 주는 값이고, 손으로 쓴 `law.py:89`·`scas.py:104`가 이를 직접 계산하는 것은 우리 시뮬레이터가 원시 상태(쿼터니언·NED 속도)를 넘기기 때문이다. 그 변환은 생성 코드 밖(통합 계층)에 남는다 — 쿼터니언 연산을 탑재 코드에 넣지 않아도 되고, FCC의 실제 인터페이스와도 맞는다

### 2.3 계층 구조

**[확정]** 최종 형상은 C++ 코어 + Python 해석층 하이브리드. 단, 개발 전략은 **Python 레퍼런스 우선** (모듈 문서 03 §7.1 채택):
- Phase 1~4의 코어(제어 요소·보간·환경·플랜트·법칙 실행부)는 순수 Python(NumPy/SciPy) **레퍼런스 구현**으로 완성. 코어 경계 API를 고정해 두고, 성능이 문제 되는 시점(몬테카를로·배치 규모 확대)에 pybind11 기반 C++ 구현으로 **드롭인 교체**
- Python 레퍼런스판은 폐기하지 않고 C++판의 **회귀 기준(oracle)** 으로 상시 유지 — 이중 구현이 아니라 검증 자산
- **[TBD]** **제어법칙 C 백엔드로의 전환 시점** — 제어법칙 구조가 굳은 뒤로 미룬다. 01 §7 미결 중 엘레본 믹싱 비율·경로추종 알고리즘·모드 전환 조건·저고도 플레어는 **구조를 바꾸는** 항목이고, 공력 DB·작동기 특성·실속 경계가 아직 [기본값] 가정값이다. 굳지 않은 설계를 C로 먼저 쓰면 C를 계속 고치게 된다 — 개발 속도가 아니라 재작업 비용이다. 구조 정본이 IR이므로(§2.2) 이 전환은 다시 쓰기가 아니라 **백엔드 추가**이고, 그래서 미뤄도 부채가 되지 않는다

| 계층 | 언어/기술 | 담당 |
|---|---|---|
| 코어 라이브러리 | C++ (선행: Python 레퍼런스) | 공력 DB 보간, 6DOF 운동방정식, 작동기, 추진, 대기모델, 적분기, 제어법칙 실행 |
| 바인딩 | pybind11 | C++ 코어의 Python 노출 |
| 해석층 | Python | 트림(scipy.optimize — SLSQP/trust-constr), 선형해석(python-control), 배치실행, 플롯·리포트 |
| 백엔드 | Python + FastAPI **[확정]** | REST API, 작업 실행(배치 트림/시뮬), 웹소켓 진행률, 결과 저장 |
| 프론트엔드 | 웹 — 바닐라 ES 모듈, no-build **[확정]** | §4 웹 UI |

- 근거: 시뮬 속도 + 추후 실시간/HIL 모듈에서 검증된 동일 C++ 코드 재사용 + 해석은 Python 생태계
- **[확정]** 실시간성 고려: 코어는 동적할당 최소화 등 실시간 친화 코딩 스타일
- **[확정 설계원칙]** **엔진 우선**: 엔진을 CLI/Python API로 먼저 완성, 웹 UI는 동일 API를 소비하는 별도 층 (UI가 엔진 개발을 막지 않도록)
- **[확정 설계원칙]** **샘플레이트 최상위 파라미터화**: 이산화 계수 하드코딩 금지, 주기(100→50 Hz 등)로부터 자동 계산. 주기 변경 시 이산 제어기 포함 위상여유 재계산 + 응답 비교 자동화 기능 제공
- **[확정 설계원칙]** **레지스트리 + 파라미터 스키마**: 교체 가능 컴포넌트(경로추종 알고리즘, 적분기, 필터 등)는 자신의 파라미터 정의(JSON 스키마)를 노출. UI는 스키마로부터 폼 자동 생성 — 새 컴포넌트 추가 시 UI 코드 수정 불필요

### 2.4 설계값 연계·정량 영향성 평가

**[확정 — 신규 요구]** 모듈 간 설계값은 유기적으로 연계되며, 어느 단계의 수치 변경이든 전체 출력에 미치는 영향을 정량 평가할 수 있어야 한다.

- **계보(lineage) 추적**: 모든 설계 산출물(트림표, 선형모델, 마진 맵, 시뮬 결과)은 자신이 계산된 파라미터 스냅샷의 **지문(fingerprint)** 을 기록한다 (§5.5 파라미터 계층이 지문 발급)
- **[확정]** 지문 대상 확장: ParamSet뿐 아니라 **입력 데이터 파일(공력 DB, 실속 경계 테이블)의 해시·버전**도 산출물 계보에 기록 — "어떤 DB 버전 + 어떤 파라미터 버전으로 계산됐는지" 재현 가능해야 Simulink 회귀 대조(§7)가 성립
- **설계 파이프라인 DAG**: params → 트림 → 선형화 → 마진 → 시뮬의 산출물 의존 그래프를 관리. 파라미터 변경 시 영향받는 산출물만 자동 무효화·증분 재계산
- **정량 Δ리포트**: 변경 전/후 두 파라미터 스냅샷에 대해 파이프라인을 재실행하고 차이를 정량 비교 — 트림점 이동, 고유치·감쇠비·이득/위상여유 변화, 응답지표(오버슈트·정착시간)·경로오차·실속마진 변화
- **파라미터 스윕/민감도**: 지정 파라미터를 격자로 흔들어 출력 민감도 산출 (배치 실행 재사용)
- 기존 100 vs 50 Hz 마진 비교(§2.3 샘플레이트 원칙)는 이 기능의 특수 사례로 흡수
- **[확정] 영향 매핑은 손으로 적지 않는다** — "이 게인이 저 노드를 건드린다"를 표로 두면
  구조 정본(M16 IR)이 바뀔 때 조용히 낡는다(§5.5 중복 정의 금지가 막는 것이 이것이다).
  파라미터를 흔들어 법칙을 **재조립하고 노드 서명을 diff** 해서 매핑을 유도한다. 부수
  효과로 **구조를 바꾸는 파라미터**도 잡힌다 — `k_thr_turn` 0→ε은 노드 4개를 새로
  만들고(안 쓰이는 경로는 생기지 않으므로) 그건 민감도가 아니라 위상 변경이다
- **[확정] 세 상태를 구분해 보인다** — 같은 "도달"이라도 성격이 다르고, 뭉뚱그리면
  사용자가 "왜 편집해도 안 먹지"를 혼자 알아내야 한다:
  ① **덮임** — 게인 스케줄이 붙은 SCAS 상수는 그래프에 있지만 실행기가 매 스텝 게인
  포트로 덮어쓴다(`inst.step(u, **gains)`) → 구조 diff에는 잡히고 런타임 효과는 0
  ② **미방출** — 스케줄된 `k_rate`는 상수 대신 조회 노드를 쓰므로 그래프에 **없다**
  ③ **법칙 밖** — 항법·작동기·유도는 IR 바깥이라 개루프가 못 본다. "영향 없음"이 아니다
- **[확정] 도달성은 크기가 아니다** — 포화된 가지나 0 게인을 지나는 경로도 도달로 잡힌다.
  「얼마나」는 개루프 Δ(2단)·폐루프 스윕(3단)의 몫이고, 화면이 그 구분을 말해야 한다
- 담당: 모듈 문서(03) M15 `pipeline`

## 3. 모듈 구성

**[확정]** 현행 Simulink 서브시스템 구조를 따라 아래 6개 모듈로 구성한다.

| 모듈 | 담당 | 주요 요소 |
|---|---|---|
| 1. Flight Control | SCAS, 오토파일럿, 게인 스케줄링, 엘레본 믹싱(제어 할당) | PID, State-Space, 필터, 게인 테이블 |
| 2. Guidance | 경로 생성·추종, 비행모드 실행기 | 경로추종 알고리즘, 웨이포인트 로직, 모드 테이블 |
| 3. Navigation | 항법 상태 추정 출력 제공 | **등가 오차 모델** (§3.1) — EKF 미구현 |
| 4. Actuator | 작동기 동특성 | Transfer Function/State-Space, Rate Limiter, Dead Zone, Backlash |
| 5. Sensor | IMU/GPS 모델 | 노이즈, 지연 (초기에는 이상 센서) |
| 6. Aircraft Dynamics | 6DOF, 공력, 추진, 대기·중력·지구모델 | Lookup Table, Quaternion, 좌표변환, 적분기 |

### 3.1 항법(Navigation) 모듈 — 등가 오차 모델

**[확정]** 툴은 EKF를 구현하지 않는다. Navigation 모듈은 실제 항법(EKF) 출력의 **통계적 특성을 재현하는 등가 오차 모델**로 구현한다.

```
항법 출력 = 참값(6DOF) + 오차(백색잡음 + 상관잡음/바이어스) + 지연
```

**근거**
- 툴이 EKF를 구현해도 실제 비행할 항법팀의 EKF와는 다른 근사물 → 현실성 착시만 유발
- 등가 오차 모델은 실제 EKF의 **성능 수치**(공분산, 지연)를 그대로 반영 가능 → 재구현보다 실제에 근접
- 제어루프가 느끼는 것은 제어대역 대비 빠른 잡음과 느린 바이어스뿐이며, 추정기 내부 구조는 제어기 설계에 영향 없음
- 항법은 별도 전문 영역 — 중복 개발 회피

**인터페이스 개방**: Navigation 모듈은 교체 가능 컴포넌트(레지스트리, §2.3)로 등록하여, 추후 항법팀의 실제 EKF 모델이나 비행소프트웨어 코드를 그대로 꽂을 수 있게 한다.

**오차 모델 파라미터** (상태별로 정의)
| 항목 | 내용 |
|---|---|
| 백색잡음 | 표준편차 (위치/속도/자세/각속도별) — **위치·속도는 수평(N·E)/수직(D) 분리** (01 §2.5 [기본값]) |
| 바이어스 | 초기 바이어스 + 상관시간(1차 마르코프) — 위치축, **수평/수직 분리** |
| 지연 | 항법 출력 지연 (transport delay) |
| 갱신주기 | 항법해 출력 주기 |

**[기본값]** 초기값은 GPS/INS 통합항법의 일반적 수준으로 시작한다. 항법팀의 실측·공분산 해석 자료 확보 시 대체.
- 위치: 수 m 급 (수평/수직 구분)
- 속도: 0.1~0.5 m/s 급
- 자세(롤·피치): 0.1° 내외 / 방위: 롤·피치 대비 큼
- 지연: 수십 ms 급
- 갱신주기: 제어주기(100 Hz)와 동등하거나 낮음 → 멀티레이트 처리 필요
- **[TBD]** 구체 수치는 항법팀 자료 확보 시 확정. 위 값들은 자리표시자(placeholder)

## 4. 웹 UI

**[확정]** UI는 웹 기반. Simulink식 자유 블록 배선 에디터는 **만들지 않는다** — 제어 아키텍처는 고정이므로 웹은 파라미터·옵션·데이터 편집과 결과 열람의 얼굴.

편집 수준 **[확정]** (a)+(b), (c)는 제외:
- (a) 파라미터·게인·트림 케이스·웨이포인트 편집 + 고정 구조 신호흐름 시각화
- (b) 제한된 컴포넌트 교체 (레지스트리 등록 대안 중 선택)
- (c) ~~자유 블록 배선~~ — 스코프 제외

**[확정]** 진입 UI는 **블록 다이어그램 허브** (Simulink "다이어그램에서 시작" 대응): 고정 구조 신호흐름 다이어그램(SVG — 설계 순서 ①~⑤ 중첩 점선 프레임, 게인 스케줄 점선 배선, 항법 피드백 경로 포함)이 첫 화면이고, 블록·프레임 라벨 클릭 → **서브시스템 하위 페이지**(#blocks/<id> — Simulink 서브시스템 더블클릭 대응, 브라우저 뒤로가기로 상위 복귀): 브레드크럼 + 내부 블록도(포트·합산점·이득 경로 — 엔진 구현과 1:1, 코드에 없는 구조는 그리지 않음) + 설계 노트([확정]/[기본값]/[TBD] 칩 — fcs-context 태그와 동일 의미) + 파라미터 패널. 파라미터 패널: ① 레지스트리 스키마 폼/열람 표(§2.3 폼 자동 생성 — 단위·범위·기본값, 파라미터 보유 법칙 컴포넌트 Autopilot·ScasAxis·Mixer를 레지스트리 "fcl" 카테고리에 등재) ② 정본 편집 화면 이동(게인 스케줄·SCAS→게인 탭, 미션플래너·유도·작동기·항법→시뮬 탭, α 리미터→엔벨로프 탭, 플랜트→트림 탭). 폼 편집(시뮬 주입)은 **주입 경로가 있는 블록만** 허용 — 현재 오토파일럿(전체 kwargs 교체, 시뮬 탭 '편집 AP') — 진실 이원화 방지. 캠페인 단위 작업(트림 격자·마진 맵·미션 실행)은 탭 유지.

**[확정]** 드릴다운은 **깊이 무제한(재귀)**: 하위 페이지 SVG 안의 진입 블록(`data-child`) 클릭 → 해시 세그먼트 추가(#blocks/scas/pitch/pi — Simulink 다층 더블클릭 대응). 트리는 SUBSYSTEMS children 재귀(자식은 crumb 짧은 라벨 필수, 단계 태그는 루트 상속), 브레드크럼 중간 클릭으로 임의 조상 이동. 파라미터 패널·SVG 수치 연동(data-p)은 **루트 블록 스키마 기준** — 자식 페이지에서도 동일 폼·연동 동작. 1차 페이지 범위: SCAS 층2 = 3축 서브시스템 블록 개요 → 층3 = 축별 상세(피치/롤/요 — ScasAxis 1:1) → 층4 = PI 내부(클램프 AW — 3축 공유 정의) · AP 층2 = 3채널 블록 개요 → 층3 = 채널별 상세(헤딩/고도/속도 — 명령필터·PI·클립·FF 합류·재클립). 배선·구조 정합은 테스트 가드(data-child ↔ children 양방향, data-p ⊆ 루트 스키마 — children 재귀).

담당 기능 (**[확정]** 전부 웹):
- 트림 케이스 매트릭스 정의·배치 실행·진행률 모니터링 (테이블 편집)
- 결과 대시보드: 고유치 맵, 보드선도, 감쇠비 테이블, **마진 맵**(Mach-고도-연료 격자) 인터랙티브 플롯, 트림 가능/불가·요구도 만족 여부 색상 맵(트림 판정 플래그·합격기준 판정 재사용)
- 지도 위 웨이포인트 / 임무프로파일 편집
- 시뮬레이션 재생 (엔벨로프 감시 플래그·실속 마진 시계열 포함)
- 검증 리포트 열람·공유
- **[확정] 게인 탭은 두 층이다 — 스케줄 자리 선택(형상) + 테이블 값 편집(값)** (v0.27). 위쪽이 그룹 × `kp·ki·k_rate` 격자로 **어떤 게인에 테이블을 붙일지** 켜고/끄고(01 §3.4 16자리), 아래쪽이 켠 자리만 표·차트·근사식으로 편집한다. 칸에는 설계점 상수를 함께 적어 "끄면 무엇으로 굳는지"가 보이고, 구조상 불가한 자리(속도·헤딩 `k_rate`)는 빈칸이 아니라 사유를 단 `— 불가`다 — 빈칸은 버그로 읽힌다. 자리 목록·설계 상수·제안 테이블은 서버(`GET /gains/catalog`)가 주고 정본은 엔진이다
  - 스토어 계약: `gainTables`는 **적용된 자리만 담은 dict이고 그 키 집합이 곧 스케줄 대상**이다(주입은 전체 교체이므로 키를 빼는 것이 곧 "스케줄 안 함"). **전부 끔만 이 dict로 표현할 수 없어** — 빈 dict는 서버가 422로 막고(조용한 무스케줄 방지) 필드를 생략하면 설계 기본으로 되돌아간다 — `gainScheduleOff` 신호를 따로 두고 요청에서 `with_schedule:false`로 옮긴다. 판단은 `web/js/lib/gainsched.js`(뷰는 테스트 면제)
  - 기존 소비자(구조도·시뮬)는 dict 형상이 그대로라 무변경으로 동작한다. **미연결**: 시뮬 탭은 아직 `gainScheduleOff`를 읽지 않아 '전부 끔'이 전달되지 않는다 — 조용히 다른 형상으로 돌지 않도록 게인 탭 상태줄이 그 사실을 표시한다 [TBD]
- 제어법칙 파라미터/옵션 편집 + 신호흐름 다이어그램(고정 구조 표시 — 블록 클릭 허브)
- **[확정] 생성 코드는 `Autocode` 탭 하나로 모으고, 선택은 계층으로 편다** (v0.25·v0.26, 상단 내비게이션). 선택지를 한 줄에 늘어놓으면 "무엇을 고르는 중인지"가 사라지므로 3단이다:

  | 단 | 선택 |
  |---|---|
  | 종류 | **형상코드**(설계 형상의 코드 표현 — 파라미터) · **탑재코드**(FCC에 실릴 제어법칙 코드) |
  | 형상코드 | 대상 `통합(형상 전체)` · 블록별 → 형식 `Python` · `C 헤더` |
  | 탑재코드 | 보기 `통합(전체 이어보기)` · `모듈별` → (모듈별이면) **역할로 묶인** 파일 탭 |

  기본은 **탑재코드 · 통합**이다 — 이 탭의 주 질문은 "지금 형상에서 무슨 코드가 실리나"이고 그 답은 파일 하나가 아니라 전체다. **통합 열람본은 전 파일을 읽는 순서대로 이어붙인 것이고, 머리말에 "빌드 단위가 아니다"를 박는다** (안 적으면 그대로 컴파일하려 드는 사람이 반드시 나온다). 형식 탭은 종류에 맞는 것만 노출한다(탑재코드는 하나뿐이라 줄 자체를 만들지 않는다 — 고를 게 없는 버튼은 잡음이다). 생성 응답은 요청 본문(=형상)을 키로 캐시해 통합↔모듈별 전환에 서버를 다시 부르지 않는다. 구조도 탭의 블록별 `[코드 생성]`은 맥락 안에서 쓰는 지름길로 유지
- **탑재 C 열람** (코드 패널 3번째 탭, v0.24): 현재 형상의 제어법칙 C를 엔진이 생성해 보여 준다. 파일별 서브탭은 **서버가 읽는 순서를 정해** 준다(진입점 → 자료형 → 조립부 → 서브시스템 실행 순서 → 파라미터 데이터 → 공용 런타임; 짝은 `.h` 먼저) — 생성물이 16개라 "어디부터 보나"가 곧 사용성이다. 형상 지문·제어주기를 함께 표시하고, **제어법칙이 아닌 블록(작동기·항법)은 탑재 C에 없다고 명시**한다(없는 게 정상임을 화면이 말해 주지 않으면 "내가 고친 게 왜 없지?"가 된다). 추적성 표는 파라미터→코드 라인 대응이라 이 탭에서는 감춘다
- **[확정] 영향성 탭**(`#influence`, v0.28) — 파라미터 66개 → IR 노드 59개(위상 16층) →
  법칙 출력 7개 → 설계 지표 8개를 한 화면(165 노드·264 간선)에 놓고, 파라미터를 고르면
  그 영향 원뿔이 층을 타고 번지는 것을 애니메이션으로 보인다. **층 번호가 곧 IR 실행
  순서이자 생성 C의 문장 순서**라 이 애니메이션은 장식이 아니라 실행 순서의 그림이다.
  기본 배치는 **전파 폭포**이고 「프로세스 뷰」 버튼이 레이어 활성망(층별 실행
  순서)으로 전환한다 — 영향 성운(radial)은 후보에서 삭제됐다. 재생 일정은 배치와
  무관한 위상 랭크라 두 배치가 같은 순서로 켜지고, 캔버스 아래 **전파 경로 패널**
  (층 칩)이 재생과 같은 박자로 켜진다. 층 상세(노드·경로 포트·설명)는 칩을
  클릭했을 때만 열린다 — 자동으로 띄우면 설명 길이 차이로 화면이 계속 출렁인다.
  파라미터를 고르면 **모두 어두워지고 원뿔이 층을 타고 한 줄씩 연결된다**(층 슬롯 150 ms
  + 층 안 시차 30 ms · 간선 성장 180 ms · 총 ~2.5 s). 완성되면 3.8 s 동안 켜진 채
  입자가 흐르고 되감기 페이드 뒤 다시 반복한다. 층 안 시차는 **IR 선언 순서**를 따르므로
  애니메이션은 층 사이뿐 아니라 층 안에서도 생성 C의 문장 순서다. **재생 시각은 배치와
  무관하게 위상 랭크로만 매긴다**(`lib/influenceplay.js`) — 전파 폭포의 `ranks`는 모듈
  밴드의 **열 인덱스**라 간선 방향으로 단조가 아니고, 그걸 시간축으로 쓰면 간선이 거꾸로
  자란다. 좌표만 배치에서 오고 시간은 `assignRanks` 하나에서 온다. 층 간격(150 ms)보다
  간선 성장(180 ms)이 길어 **인접 층은 겹치는데 이건 설계다** — 엄격 직렬화는 4.8 s가 걸려
  요구와 양립하지 않는다. 대신 노드 점등을 120 ms 램프로 만들어 "어두운 노드에서 선이
  나가는" 장면을 없앤다. 캔버스가 말하는 진행은 **글로도 남는다**: 재생 중 캔버스 아래
  「층 7/21 · limiter_active 도달」, 완료 후 상세 패널에 「층 3에서 시작해 층 13에서
  limiter_active 도달 · 21층 중 19층 통과」. **`aria-live`는 쓰지 않는다** — 보조 표면이
  정본(표)보다 더 말을 걸게 되고, 무한 반복이라 영구히 도배된다.
  스킨은 Apple 다크모드(중립 흑회색 표면 + 시스템 색)이고 캔버스 안에서 색으로
  존재하므로 `app.css`를 건드리지 않는다 — 팔레트 정본은 `lib/influence.js`.
  노드는 종류마다 윤곽이 다르다(파라미터 스퀘어클·출력 캡슐·지표 링) — 색 하나에만
  기대면 색각 이상에서 구분이 통째로 사라진다. 밝은 패널에 얹는 배지·주석은
  같은 색의 접근성 변형(`STATE_INK`)을 쓴다(흰 바탕에서 다크 색은 3:1 밑으로 떨어진다).
  **표가 정본 표면이고 캔버스는 보조다** — 캔버스는 보조기술에 불투명하므로 화면이
  말하는 모든 사실(상태·도달 개수·도달 출력)이 아래 표에 그대로 있다(지도 편집기가
  웨이포인트 표에 접근성을 맡긴 것과 같은 규약).
  **[확정] 2·3단은 케이스 격자가 단위다** (v0.29) — 마진 맵과 같은 격자 입력(mach
  범위 × 고도·연료 목록, 기본 15케이스). 2단(개루프 Δ)은 케이스당 ~10 ms라 격자
  전체로 돌고, 화면은 (손잡이×루프) 최악 Δ와 그 케이스를 요약 정본으로, 케이스별
  전체 표를 접힌 근거로 둔다. 3단은 케이스 × 런 비용이 곱이라 두 단계다:
  **A 전 케이스 스캔**(`POST /influence/scan`)이 케이스마다 base 런 하나로 지표를
  재고(`sweep_plan(shape, [], ())` = base 계획 — run_sweep 그대로 재사용)
  `diagnose_grid`(규칙 4 — "격자 런이 생기면 소급 활성화되는 별도 입력"이라던 그
  함수)가 지표별 정상/국소/전역과 결함 케이스를 판정한다 → **B 부분 풀 스윕**(기존
  `/influence/sweep`)은 스캔이 고른 결함 케이스(체크박스로 조정)에만 처방 부분공간을
  흔든다. 케이스 이름은 웹이 명시 부여한다 — 판정의 bad_cases(이름 문자열)를 B의
  케이스 객체로 되돌리는 매핑이 서버 자동 명명 형식에 묶이지 않게. 영향성 라우트의
  케이스 상한 200은 서버 검증이다(오타 격자가 단일 워커를 시간 단위로 점유하지 않게)
- 공력 DB 뷰어 (§4.2)

운영 형태:
- **[확정]** **단독 사용자, 로컬 서버**. 다중 사용자·인증·동시 작업 큐·편집 충돌 처리 스코프 제외 (추후 확장 여지)
- 폐쇄망 이점: 서버 1대 배포로 브라우저 접속 → 개별 PC 툴체인 설치 대비 반입 유리
- **지도 타일**: 폐쇄망에서 인터넷 타일(OSM 등) 불가 → 오프라인 타일 서버 반입 또는 위경도 격자+해안선 벡터 폴백 모드를 처음부터 설계
- **[확정]** 프론트엔드 스택: **순수 바닐라 ES 모듈 + no-build, 외부 의존 0** — 근거: ① 폐쇄망 반입물 = `web/` 파일 전부, 현지 수정이 텍스트 에디터로 가능(빌드 툴체인 반입 불요) ② eval-free — 엄격 CSP(unsafe-eval 금지) 환경에서도 동작(런타임 템플릿 컴파일 프레임워크 배제 사유) ③ 반입 심사 가능한 비-미니파이드 소스. 플롯은 자체 Canvas — uPlot 등 단일 파일 vendored 반입은 필요 시 백로그. 서버(FastAPI StaticFiles)가 정적 서빙 — 서버 1대 = 배포 전체
- **[기본값]** 웹 테스트 전략: 로직 모듈은 공존 `*.test.js`를 node 내장 러너(`node --test`, npm 의존 0)로 — **개발 환경 전용 도구**(반입물 아님). DOM 조립 전용 모듈은 얇게 유지하고 실서버 데모로 검증

## 5. 데이터 인터페이스

### 5.1 공력 DB 입력
- **[확정]** CSV / Excel → pandas 기반 판독기 + C++ 코어용 내부 포맷 변환기
- **[TBD]** 보간 방식(선형/스플라인), 범위 밖 외삽 정책, 테이블 차원·크기

### 5.2 공력 DB 뷰어 (교차확인 기능)
- **[확정]** 조건별 CL/Cm 곡선 등 플롯 기능 제공 — 보간 엔진 재사용으로 저비용
- 용도: DB 임포트 시 데이터 이상 검사, 공력팀 제공 실속 경계값과의 교차확인
- **실속 경계 정본은 공력팀 제공 테이블** (도메인 문서 §2.3). 툴 추출값은 참고용

### 5.3 실속 경계 테이블
- 입력 형식: α_stall = f(Mach, 형상조건 등) 테이블 — 공력팀 제공
- 소비자: 엔벨로프 감시(§6.1), 대시보드 엔벨로프 표시, α 리미터
- **[TBD]** 파일 규격 (공력팀 협의)

### 5.4 설정/케이스 정의
- **[확정]** 포맷: **YAML**(주석·diff 친화) + **JSON 스키마 검증** — 웹 편집과 파일 기반 편집이 같은 스키마를 공유 (스키마는 엔진 params 모듈이 정의·발급)
- **[TBD]** 게인·설계 데이터 형상관리 방식 (git 사용 제안)

### 5.5 파라미터 관리 계층 (Data Dictionary 대체)

**[확정]** Simulink Data Dictionary가 담당하던 변수 관리를 대체하는 계층을 둔다.

- 관리 대상 예: 제어 게인(Kp, Ki 등), 기하(WingArea, CG), 질량·관성(Mass, Ix, Iy, Iz), 작동기·엔진 파라미터, 리미터 값, 검증 합격기준 수치(오버슈트·정착시간·경로오차 허용치 등 — 도메인 문서 §5)
- 요구사항:
  - 단일 정의 원본에서 C++ 코어 / Python 해석층 / 웹 UI가 모두 참조 (중복 정의 금지)
  - 단위·설명·유효범위를 메타데이터로 보유 → 웹 UI 폼 자동 생성 및 입력 검증에 활용
  - 연료 소모에 따른 질량·CG·관성 변화는 시간·상태 의존 파라미터로 취급
  - 텍스트 기반 파일로 저장하여 버전관리(diff) 가능
- **[확정]** "중복 정의 금지"의 집행 규칙 — **웹은 엔진 기본값을 재기술하지 않는다**: 요청에 사용자가 실제로 정한 값만 싣고 나머지는 엔진 ParamDef가 채우게 한다(부분 지정 보충). 화면에 즉시 값을 보여야 해서 폴백 사본이 불가피한 경우(폼 프리필)는 레지스트리 스키마가 도착하면 사용자가 손대지 않은 값만 갱신해 스스로 정렬한다. 근거: 시뮬 탭이 항법 기본값 9개를 재기술해 그중 7개가 엔진과 어긋난 채(pos_std 1.0↔3.0, update_hz 50↔100 등) 돌던 사례 — 블록도는 엔진 값을 표시하는데 실행은 다른 값이라 무증상이었다 (v0.18)
- **[확정]** 파일 포맷 YAML + JSON 스키마 (§5.4와 동일 규격). 파라미터 스냅샷마다 **지문(fingerprint)** 발급 — 산출물 계보 추적·영향성 평가(§2.4)의 키
- 합격기준을 파라미터로 관리하면 판정 기준이 지문에 포함됨 — 산출물 계보에 요구도 버전이 자동 반영되고, 기준 변경 시 Δ리포트(§2.4)로 판정 변화 추적 가능

## 6. 시뮬레이션 프레임

- **[확정]** 플랜트 적분 주기와 제어기 이산 주기 **분리** (멀티레이트)
- **[기본값]** 고정스텝 RK4로 시작
- 시나리오: 임무프로파일 입력 → 모드별 유도 → 폐루프 6DOF
- **[TBD]** 몬테카를로 분산 대상·범위 — 후보: 공력계수 오차(±%), 질량·CG, 작동기 특성(대역폭·지연), 항법 오차 시드, 바람/난류(확장 시). 산출 통계에 합격기준별 **요구도 만족률**(예: 안정성·오버슈트 만족 비율) 포함. Phase 4 sim 구현 시 확정

### 6.1 엔벨로프 감시 (필수 기능)
- **[확정]** α, β, Mach의 DB 유효범위 이탈 즉시 플래그 (외삽 결과 무의미 — 실속 판정과 별개 플래그)
- **[확정]** 실속 마진(α_stall − α) 시계열 기록·표시
- **[확정]** 임무프로파일 전체 최악 마진 요약 리포트

## 7. 툴 검증 전략 (3층)

반출 가능 자료가 거의 없음 → **데이터 반출 대신 툴을 폐쇄망에 반입**하는 전략.

| 층 | 대상 | 방법 | 장소 |
|---|---|---|---|
| 1. 공개 벤치마크 | 엔진(트림·선형화·6DOF) | Stevens & Lewis F-16 모델의 공개 공력테이블·트림·선형화 결과 재현 | 개방망 |
| 2. 물리 검증 | 수치 정확성 | 에너지 보존(무저항), 스텝 반분 수렴 차수, 미소섭동에서 비선형 vs 선형모델 일치 | 개방망 |
| 3. Simulink 대조 | 자사 기체 모델 | 내장 **회귀검증 모듈**: Simulink 결과 CSV(트림표, A/B/C/D 행렬, 시계열) 판독 → 허용오차 비교 → 리포트 자동 생성 | 폐쇄망 |

## 8. 사용자 워크플로우

**[확정]** 주 인터페이스는 웹 UI. 모든 단계는 CLI/Python API로도 수행 가능(엔진 우선 원칙).

1. 공력 DB(CSV/Excel) + 실속 경계 테이블 + 기체 파라미터 로드 → DB 뷰어로 이상 검사
2. 웹에서 트림 케이스 매트릭스 정의 → 배치 트림(진행률 모니터링) → 결과표·플롯
3. 트림점별 선형화 → 안정성·마진 맵 대시보드
4. 게인 스케줄 설계 — 웹에서 **스케줄 자리 선택**(형상) 후 테이블 값 편집
5. 지도에서 웨이포인트 임무프로파일 편집 → 폐루프 시뮬 → 재생 + 엔벨로프 감시
6. 검증 리포트 생성·열람 (벤치마크/회귀 비교 포함)

## 9. 구현 측 TBD 백로그

| 항목 | 관련 절 | 결정 시점 |
|---|---|---|
| 항법 오차 모델 구체 수치 | 3.1 | 항법팀 자료 확보 시 (그 전엔 일반값) |
| 보간 방식·외삽 정책 | 5.1 | 구현 시 |
| 실속 경계 테이블 파일 규격 | 5.3 | 공력팀 협의 시 |
| 게인·설계 데이터 형상관리 방식 | 5.4 | 워크플로우 구체화 시 |
| ~~프론트엔드 스택 선정~~ → 확정 (§4 바닐라 ESM no-build) | 4 | 완료 |
| 오프라인 지도 방식(타일 서버 vs 벡터 폴백) | 4 | 폐쇄망 적용 검토 시 |
| 미발굴 Simulink 기능 추가 조사 | 2.1 | 현행 모델 열람 가능 시 |
| 몬테카를로 분산 대상·범위 정의 | 6 | Phase 4 sim 구현 시 |
| Simulink 대조 허용오차 수치 | 7 | 폐쇄망 검증 시 |
| **생성 코드 수정 경로** | 1·2.2 | FCC팀 인터페이스 협의 시. 생성물은 그대로 실리지만 FCC팀이 신뢰성 시험 후 일부를 고칠 수 있고, 그 수정은 **재생성 시 소실된다**. 사용자 코드 삽입 지점을 열지, 특정 시점에 baseline으로 소유권을 이관할지 미결. MATLAB 세계의 원칙("생성 코드는 고치지 말고 모델을 고쳐라")은 현실에서 잘 안 지켜진다 — **안 정해두면 "재생성하니 팀 수정이 사라졌다"로 터진다** |
| ~~대기속도 이중 계산 정리~~ → 완료 (v0.22) | 3 | IR 이식 중 발견 — 오토파일럿만 `norm(nav.vel_n)`으로 대기속도를 따로 구해 나머지(마하·SCAS·리미터)의 `airdata_from_nav`와 2.8e-14 어긋나 있었다. **`airdata_from_nav` 하나로 통일** — (V, α, β) 계약을 함께 내는 함수이므로 바람 모델이 들어올 때 고칠 곳도 거기 하나다. 폐루프 회귀 핀은 움직이지 않았다 |
| **인증 등급(DAL) 확인** | 1 | 체계 요구 확정 시. float32 강제 여부·MISRA C 준수 수준·구조 커버리지(MC/DC) 요구·도구 인증 필요성을 전부 좌우한다. 현재 생성 코드는 배정밀도이며 이는 대조 차이를 "알고리즘 오류"와 "정밀도 손실"로 분리하기 위한 **잠정 선택**이다 [기본값] |

---
*문서 이력:*
- *v0.1~v0.3 — 통합 문서(fcs-design-tool-context.md)로 관리*
- *v0.4 — 제어법칙/구현 2개 문서로 분리. 공력 DB 뷰어(교차확인용) 기능 추가, 실속 경계 = 공력팀 제공 정본으로 확정*
- *v0.5 — MATLAB/Simulink 기능 대체 매핑표 신설(§2.1). Stateflow 미사용 확정*
- *v0.6 — 현행 사용 블록·툴박스 조사 반영: 매핑표 확장, 제어 요소 라이브러리(§2.2)·모듈 구성(§3)·파라미터 관리 계층(§5.5) 신설*
- *v0.7 — 스코프 확정: LQR 제외, 자동 PID 튜닝/Parameter Estimation 제외, EKF 미구현(등가 오차 모델 + 인터페이스 개방). 항법 오차 모델 파라미터 정의*
- *v0.8 — 언어 전략 확정: Python 레퍼런스 우선 → C++ 드롭인 (모듈 문서 03 §7.1 채택). 설계값 연계·정량 영향성 평가 요구 신설(§2.4). 설정/파라미터 포맷 YAML + JSON 스키마 확정*
- *v0.9 — 범용 설계툴 아키텍처 문서 대조 결과 반영: 몬테카를로 분산 대상 [TBD] 등재(§6), 검증 합격기준 수치를 파라미터 관리 대상에 포함(§5.5)*
- *v0.10 — 계보 지문 대상에 입력 데이터(공력 DB·실속 경계) 해시·버전 포함(§2.4), 몬테카를로 산출에 요구도 만족률 통계 명시(§6), 대시보드에 트림 가능/요구도 만족 색상 맵 추가(§4), 절 번호 중복 정리(사용자 워크플로우 §7→§8, 백로그 §8→§9)*
- *v0.11 — M13 서버 구현으로 백엔드 스택 FastAPI 확정(§2.3). 진행률·협조적 취소는 엔진 진행 콜백(trim_batch·Simulator.run on_progress) 단일 경로, 결과 저장은 JSON + 비유한값 정책(NaN→null, ±inf→"inf" 문자열, 복소 고유치→[re,im]) [기본값]*
- *v0.12 — M14 착수: 프론트엔드 스택 확정(§4 — 바닐라 ESM no-build·의존 0·eval-free, CSP·폐쇄망 현지 수정·반입 심사 근거). 웹 테스트 전략 [기본값](node 내장 러너 — 개발 환경 전용) 등재*
- *v0.13 — 블록 다이어그램 허브 확정(§4): 구조도 = 진입 화면, 블록 클릭 → 스키마 파라미터 폼(레지스트리 "fcl" 카테고리 등재) / 정본 편집 화면 이동. AP 파라미터 폼→시뮬 주입 경로 개통 (스키마 기반 폼 자동 생성 백로그의 1차 실현)*
- *v0.14 — AP 주입 검증을 레지스트리 ParamDef 경유로 승격 [기본값] (§4·§5.5): 미정의 키·타입·범위·choices를 제출 시점 422로 판정(서버는 유한성만 — NaN은 ParamDef 범위 비교를 통과하므로), boolean/enum 파라미터 컴포넌트도 주입 수용 가능. ParamDef·생성자 기본값 일치는 엔진 테스트가 드리프트 가드. 블록 파라미터 폼 필드를 기능 그룹(루프·한계 등)으로 재배열 — 표시 전용(FIELD_GROUPS)*
- *v0.15 — 구조도 허브를 시뮬링크 스타일 드릴다운으로 개편(§4): 최상위 SVG(설계 순서 ①~⑤ 중첩 프레임·게인 스케줄 점선 배선·피드백 경로) + 블록별 서브시스템 하위 페이지(#blocks/<id> — 브레드크럼·내부 블록도·설계 노트 칩·파라미터 패널). 내부 블록도는 엔진 구현과 1:1(SCAS 평탄형 PI+k_rate, AP 명령필터·한계 클립·선회 FF, α 리미터 동적 상한 등). 참조 디자인: control_law_block_diagram HTML*
- *v0.16 — 드릴다운을 깊이 무제한(재귀)으로 확장(§4 [확정]): children 트리 + data-child 진입 블록 + 다단 브레드크럼(#blocks/scas/pitch/pi). 1차 범위 — SCAS 3축(층3)·공유 PI 내부(층4), AP 3채널(층3, 층2는 채널 블록 개요로 재구성 — 기존 상세는 층3으로 이관). 사용자 지적("시뮬링크는 하위의 하위 블록도 있는데 2층뿐") 반영*
- *v0.17 — 웹 "코드 생성" 신설(§1 스코프 단서·§4): 편집된 파라미터를 반영한 Python(엔진 1:1 실행 가능)·C 파라미터 헤더 + 검토 패널(기본값 대비 변경 Δ·한계 근접/정밀도 주의·파라미터→스키마→코드 라인 추적성 표). MATLAB 코드젠의 역방향 — 모델을 코드로 바꾸는 것이 아니라 코드가 정본인 형상을 코드 표현으로 꺼낸다. 생성 코드가 명명할 파이썬 심볼은 레지스트리 검증 엔드포인트(POST /registry/{cat}/{name}/validate)가 엔진 인스턴스에서 회신 — 이름 추론 금지(드리프트 0), 부수 효과로 생성자 교차 조건까지 생성 시점에 엔진이 판정*
- *v0.18 — §5.5 "중복 정의 금지"에 집행 규칙 신설(웹은 엔진 기본값을 재기술하지 않는다): 시뮬 탭이 항법 기본값 9개를 하드코딩해 7개가 엔진 ParamDef와 어긋난 채 실행되던 것을 제거 — 이제 사용자가 정한 시드(+블록 탭 적용값)만 싣고 나머지는 엔진이 보충한다. 작동기 프리필은 표시상 폴백을 남기되 스키마 도착 시 자기 정렬. 발견 경로는 코드 생성(v0.17)의 기본값 대비 Δ·센티널 경고*
- *v0.19 — 항법 오차 모델 파라미터 표(§3.1)에 위치·속도·바이어스의 수평/수직 분리 반영 (01 v0.20). 등방 가정이 수직 채널을 후하게 모사해 저고도 임무 고도 마진이 낙관적으로 나오던 것을 정정 — 30 m 씨스키밍이 RALT 없이 성립하는지 판정하기 위한 수직 오차 예산 확보가 목적*
- *v0.20 — **스코프 확대: 탑재 제어법칙 C 생성이 범위 안으로 들어옴**(§1). 종전 "FCC 탑재용 비행코드 생성 제외 — 별도 모듈로 추후 부착"을 대체한다. 전제는 생성 코드가 FCC에 통합되어 그대로 실린다는 것. 이 전제에서는 **시뮬이 실릴 코드를 돌려야** 하므로 손으로 쓴 C를 따로 두는 안(제어법칙 두 벌)은 §5.5 중복 정의 금지와 충돌해 철회했다. 구조의 정본은 IR 하나이고 Python 실행·C 생성은 백엔드(§2.2, M16) — 블록 25종만 이중 구현이며 이는 집합이 고정·유한해 블록별 대조로 관리된다(MathWorks도 시뮬 엔진과 TLC로 이중 구현하고 SIL/PIL로 대조). C 백엔드 전환 시점은 제어법칙 구조가 굳은 뒤로 미룸 [TBD §2.3] — 01 §7 미결 중 믹싱 비율·경로추종·모드 전환·플레어가 구조를 바꾸는 항목이라 지금 C로 굳히면 C를 계속 고치게 된다. 이산 계수는 생성 시점에 구워 libm 호출을 없애고(부작용: dt가 형상의 일부), 생성은 결정적(시각 미포함). **한계 명기**: FMA 축약만 켜도 비트 일치가 깨진다(측정 2.8e-16) — 생성 헤더가 빌드 요구를 싣되 타깃 확인(PIL)은 FCC팀 몫. §9에 [TBD] 2건 등재(생성 코드 수정 경로, 인증 등급 DAL). 계기: 내가 v0.17에서 §1에 "웹 코드 생성은 로직을 생성하지 않는다"는 [확정] 문구를 원래 없던 채로 추가해 경계를 못박았고, 사용자가 "제어법칙 코드는 실릴 것"임을 지적하며 드러남*
- *v0.21 — **제어법칙 전체가 IR로 표현됨**(§2.2, M16 증분 B): 오토파일럿·α 리미터·엘레본 믹서·게인 스케줄·최상위 조립까지. 판가름 항목이던 "IR이 제어법칙 전부를 표현하는가"에 **표현된다**로 답이 나왔고, 필요했던 확장은 다중 출력·enable 영역·1D 테이블 조회·연산 5종뿐이었다. 모드 분기(축 on/off)는 enable 영역으로, 항법 무효 홀드는 그래프 전체 enable로 표현된다. **입력 경계 확정**: 생성 제어법칙은 원시 항법 상태가 아니라 이미 계산된 공학량을 받는다 — 쿼터니언·에어데이터 변환은 실기 FCC에서 항법·ADC 몫이므로 생성 코드 밖이다. 검증은 실제 데모 미션 18,000 제어 틱 재생 3자 대조(비트 일치). 부수 발견 2건: ① 대기속도가 두 군데서 다르게 계산되고 있었다(§9 [TBD] 등재) ② 롤 각속도 입력 `p`가 생성 C의 파라미터 포인터를 가려 컴파일이 깨졌다 — IR이 예약 인자명을 거부하도록 보강*
- *v0.22 — **정본 이관 완료**(§2.2): `fcl/`의 클래스들이 IR 백엔드로 전환되어 구조가 `fcl/graphs.py` 한 곳에만 남았다. v0.21까지는 명령형 Python과 IR 선언이 **둘 다 구조를 갖고** 있었고(대조 테스트가 어긋남을 발견할 뿐 막지는 못하는 상태), 앞으로 믹싱 비율·경로추종·모드 전환·플레어가 구조를 바꿀 예정이라 두 벌을 유지하는 비용이 계속 커질 참이었다. 이관으로 기존 회귀 409건이 그대로 통과 — 그것이 이관이 옳았다는 증거다(손으로 쓴 오라클은 이 시점에 사라지므로, 이후의 유일한 이중 구현 대조는 IR↔생성 C뿐이다). 함께 처리: **대기속도 이중 계산 통일**(§9 해소), `CommandFilter`를 원시 블록(M2)으로 이동하고 IR 선언을 `fcl/`로 옮겨 codegen이 blocks만 의존하도록 계층 정리(03 §1). 성능 실측 +2.6%. C 백엔드 전환 시점은 §2.3 [TBD] 그대로 — 제어법칙 구조가 굳은 뒤*
- *v0.23 — **생성 C 기능축 분할 + 공용 런타임 분리**(§2.2). 종전 산출물은 59개 블록이 `fcl_step()` 한 함수(339줄)에 평탄하게 펼쳐져 FCC팀이 서브시스템 하나만 떼어 읽을 수 없었고, 공용 헬퍼(`claw_clip` 등)는 산출물마다 `static`으로 복제돼 같은 코드를 산출물 수만큼 검증해야 했다. MATLAB이 나누는 두 축 중 우리는 역할축만 갖고 있었던 셈이다. **IR은 계층화하지 않고** 연속 노드 묶음에 이름표만 붙이는 방식을 택했다 — 계층화하면 호출 규약이 IR로 올라와 "선언 순서 = 실행 순서 = 생성 C 문장 순서"를 잃는다. Python 실행기는 이름표를 읽지 않으므로 분할이 실행을 바꿀 수 없고, 실제로 **데모 미션 18,000 틱 7개 출력이 분할 전후 비트 일치**했다. 결과: `fcl.c` 339→97줄(조립부만), `sched`·`ap`·`lim`·`scas`·`mix` 5개 파일 + `claw_rt.c/.h`. **형상 지문은 움직이지 않았다**(`c0f9af6f848059c4`) — 배치가 아니라 법칙의 신원이므로 그게 맞고, 그 성질을 테스트로 못박았다. IR이 새로 거부하는 것 4가지: 이름표 불연속·부분 태깅·enable 영역이 경계를 가로지름·이름표가 신호와 충돌. 웹 코드 패널 연결(증분 E)은 사용자 결정으로 보류 — 웹은 여전히 파라미터 전용 생성기다*
- *v0.24 — **웹 코드 패널에 [탑재 C] 탭**(§1·§4). 사용자가 브라우저에서 [코드 생성]을 눌러 보고 "코드가 파라미터밖에 없는데?"라고 지적한 것이 계기 — 맞았다. v0.20~v0.23으로 탑재 C 생성기를 다 만들어 놓고도 웹은 v0.17의 파라미터 전용 생성기 그대로였고, 화면 안내문도 "로직이 아니라 파라미터만"이라는 옛 문구가 남아 있었다. 이제 코드 패널이 세 탭이고, 탭마다 안내문이 다르다(두 탭이 내는 물건이 근본적으로 다르므로 같은 문구를 쓸 수 없다). **웹은 C를 조립하지 않는다** — 엔진 생성기를 서버(`POST /codegen/flight`)가 부르고, 그 응답은 커밋 산출물 `flight/gen/`과 **바이트 단위로 같다**(서버 테스트가 대조). 부수로 **조립 정본을 하나로 합쳤다**: `flight/generate.py`가 `fcl_graph(...)`를 따로 불러 게인·타면 한계·마진을 재기술하고 있었고(§5.5 중복 정의 금지 위반 — 서버까지 같은 짓을 하면 정본이 셋이 될 참이었다) `make_demo_fcl().init(dt).runner` 한 경로로 통합했다. 통합 후 산출물이 바이트 단위로 동일한 것이 등가성 증거다. UI 판단: 파일이 16개라 **읽는 순서를 서버가 정해** 주고(진입점 → 자료형 → 조립부 → 서브시스템 실행 순서 → 데이터 → 공용 런타임, 짝은 `.h` 먼저), 형상 지문·제어주기를 함께 띄우며, **제어법칙이 아닌 블록(작동기·항법)은 탑재 C에 없다고 명시**한다 — 없는 게 정상임을 화면이 말해 주지 않으면 사용자가 자기 편집이 누락됐다고 읽는다*
- *v0.25 — **생성 코드를 `AUTO CODE` 탭 하나로**(§4). 사용자 지시 "맨 윗쪽 탭에 AUTO CODE 항목을 넣고 거기로 다 이동, 통합 코드가 먼저 기본으로". 상단 내비에 탭을 신설하고 진입 시 **대상=통합(형상 전체) · 언어=탑재 C**로 연다 — v0.24까지는 구조도에서 블록을 클릭해 들어가야 했고 기본이 블록별 파라미터 코드였는데, 이 화면의 주 질문은 "지금 형상에서 무슨 코드가 나오나"다. 항목별은 `대상` 선택으로 남는다. 부수: 스펙 조립·메타를 `lib/specs.js`로 빼 뷰가 판단을 갖지 않게 했고(뷰는 테스트 면제 — 판단은 lib에), 헤더 탭 ↔ 라우터 드리프트 가드를 **실제 검사로** 바꿨다(index.html의 data-view와 main.js VIEWS를 원문에서 읽어 대조 — 종전엔 "개명 시 갱신할 것"이라는 수동 사본이라 죽은 탭을 못 잡았다)*
- *v0.26 — **Autocode 선택 계층화**(§4). 사용자 지적 "대상들이 너무 산발적이다 — 크게 형상코드·탑재코드로 나누고, 원하면 세부가 나오게. 탑재코드는 탑재되는 모든 코드가 통합되어 나오게 하되 모듈별로도 알 수 있게 하이어라키를 짜라". v0.25는 대상(통합·블록 3개)·형식(3개)·파일(16개)이 한 화면에 평평하게 널려 있어 무엇을 고르는 중인지 안 보였다. 이제 **종류 → 대상/보기 → 파일** 3단이고, 종류가 형식 탭까지 결정한다(형상코드=Python·C 헤더, 탑재코드=하나뿐이라 줄 없음). **탑재코드 통합 열람본** 신설 — 16개 파일을 읽는 순서대로 이어붙이고 머리에 형상 지문·파일 수·순서 규칙과 "빌드 단위가 아니다"를 박는다. 파일 탭은 역할(진입점·자료형·조립부·서브시스템·데이터·런타임)로 묶어 보여 준다. 탭 이름은 `AUTO CODE` → `Autocode`. 생성 응답을 요청 본문 키로 캐시해 보기 전환에 서버를 다시 부르지 않는다*
- *v0.27 — **게인 탭에 스케줄 자리 선택**(§8·§4). 사용자 질문("게인스케줄링 테이블에서 어떤 게인을 사용할지는 선택할 수 있게 해야하지 않아?")이 계기 — 답은 "엔진은 이미 자리 단위로 지원하는데 웹이 6자리를 하드코딩하고 있었다"였다. 테이블 dict의 키 집합이 곧 스케줄 대상인데(주입은 전체 교체) 웹은 `/gains/demo`가 준 이름을 그대로 열로 깔아 값만 고칠 수 있었고, 그래서 "적분 게인을 빼면?"·"요축은 왜 고정인가"·"AP 게인 스케줄"을 물어볼 수단이 없었다. AP 스케줄 배선(`fcl_graph` ap_ports)은 구현돼 있으면서 **웹에서 도달 불가능한 코드**였다. 이제 그룹 × 키 격자로 16자리를 켜고/끄며, 바뀐 형상이 `POST /codegen/flight`까지 흘러 룩업 수와 형상 지문이 실제로 움직인다. 규칙 단일화 부수 작업: 게인 이름 규칙이 세 군데(`graphs.py` 상수·`autopilot_nodes`의 하드코딩된 속도·헤딩 거부·`law.py`의 재선언)에 조각나 있던 것을 `SCHEDULABLE` 한 표로 합쳤다(§5.5 중복 정의 금지) — 덕분에 구조상 불가한 자리가 `init(dt)`의 내부 사정("rate 경로가 있는데 rate_src가 없다")이 아니라 **조립 시점에 무엇을 잘못 골랐는지로** 걸린다. 기본 형상은 불변(`flight/generate.py` 0개 갱신, 지문 c0f9af6f848059c4 유지) — 자리 선택을 도입해도 아무것도 안 고르면 예전과 같은 코드가 나온다. 겸사겸사 Autocode가 게인 탭 적용값을 **말없이** 쓰던 것도 고쳤다(각주가 스케줄 자리 수·이름·전부 끔 여부를 밝힌다) — 시뮬 탭엔 '편집 게인 사용' 토글이 있는데 Autocode엔 없어서 적용 버튼 한 번에 코드가 바뀌는 이유가 화면에 없었다*
- *v0.28 — **영향성 탭 신설**(§2.4·§4). 사용자 요구 "각 단계와 모듈과 파라미터들이 얼마나 연관되어 있는지를 딥러닝 레이어 시각화처럼 움직이는 화면으로". §2.4는 v0.8에 [확정]으로 적힌 뒤 M15에 `Pipeline`·`delta_report`만 구현돼 있었고 **화면이 없었다** — 03 §M13 후속의 "Δ리포트 API(M15 결선)"가 그 미결이다. 1단(구조 도달성)을 붙였다: 엔진 `pipeline/influence.py`, 서버 `POST /influence/structural`(65개 파라미터를 각각 재조립·diff하고도 ~70 ms라 동기), 웹 `#influence` 탭. **매핑을 손으로 적지 않은 것이 이 구현의 요지다** — 재조립 후 서명 diff로 유도하므로 `fcl/graphs.py`가 바뀌면 매핑도 따라 바뀐다. 서명 정규화(Table·ndarray를 값으로 환원)를 빼면 동일성 repr 때문에 **전 노드가 바뀐 것처럼 보이고 그림은 그럴듯하게 그려진다** — 그 오탐을 테스트가 지킨다. 제어주기(dt)는 `fcl_graph`의 인자가 아니라 러너의 인자라 노드 서명이 안 움직이므로 **러너 인스턴스 서명까지** 본다(이산 계수가 형상의 일부라는 §2.2의 귀결). 부수: M7 `make_demo_fcl`에 `scas`·`mixer`·`alpha_margin` 주입 인자 추가(기존 `autopilot=` 주입과 같은 성격 — 조립 정본을 우회하지 않기 위해 해석 모듈이 아니라 조립 함수에 손잡이를 단다), M1 레지스트리에 `param_defs()`(스키마가 "설명 [단위]"로 합쳐 놓은 것을 되파싱하지 않게). 2단(개루프 Δ)·3단(폐루프 스윕)은 배치 후보 확정 후. 스킨은 사용자 지적("색도 모양도 촌스럽다")에 따라 **Apple 다크모드**로 — 표면을 중립 흑회색으로 되돌린 것이 핵심이다(바탕이 네이비면 그 위 시스템 색이 대비를 못 얻는다). 노드 윤곽을 종류마다 다르게 준 것은 미감이 아니라 접근성이다(색 하나에만 기대지 않게). 발광 구조(절반 해상도 + 단일 blur + lighter)는 그대로 두고 입자에 반짝임만 얹었다*
- *v0.30 — **자동 PID 튜닝 스코프 번복 — 편입**(§2.1 표). 사용자 요구 "트림 자동화→게인 스케줄링 자동화→마진맵 adaptive 검증→이터레이션 전체 자동화"에 따라 "수동 튜닝으로 설계" [확정]을 뒤집는다 (번복 사유: 자동 설계 루프가 요구사항으로 승격됨). 방법은 최적화 블랙박스가 아니라 **결정론 2단 루프쉐이핑**(레이트 댐퍼는 폐쇄 모드 감쇠·대역폭 목표의 단조 스캔+이분 — 작동기·지연 포함 폐루프 고유치 안정 캡이 01 §4.2 PM −76.3° 실증 사고의 재발 방지 가드, 자세 PI는 목표 교차 루프쉐이핑 + 마진 검증 기하 백오프 + 달성 대역폭 하한) + 선택적 제한 Nelder-Mead 마무리(기본 OFF — 결정론·재현성 우선). LQR 제외는 유지 — PI 구조 불변, 튜닝 방식만 자동화. 안전장치 셋을 표에 명기: 기본 gated 승인 게이트, 에스컬레이션(상위 설계 변경) 자동 적용 금지, infeasible은 예외가 아니라 결과(원인 분류 입력). 함께: 신설 M17 `design/` 모듈(모듈 문서 §M17), 게인 페이로드 태그드 유니언(TableIn|PolyTableIn — 기존 테이블 페이로드 무변경 호환), 웹 "자동 설계" 탭. 상세 결정 기록은 01 v0.23*
- *v0.31 — **자동 튜닝 마무리의 스코프 정정**(§2.1 표): "선택적 Nelder-Mead 마무리(기본 OFF)"는 이제 절반만 맞다. 전역 polish는 그대로 기본 OFF지만, **대역폭이 무너진 실패 자리에는 구제 마무리가 항상 돈다** — 백오프가 ωc를 버려 마진을 사는 한 방향 탐색이라 "마진은 남는데 대역폭이 없는" 해에서 멈추기 때문이다(데모 infeasible 6점이 전부 그 경우: PM 103°·GM 9.6 dB인데 교차가 목표의 0.168배). 백오프 루프와 **같은 수용식**을 공유하고 대역폭 하한을 다시 통과할 때만 채택하므로 결과를 나쁘게 만들 수 없고, 통과한 자리는 건드리지 않아 결정론도 그대로다. 그 도구는 종전에도 있었으나 x0 = [0, 0]에 scipy 기본 zdelt를 쓰면 변 길이 0.025%짜리 simplex라 **켜도 아무것도 안 했다**(초기 simplex를 로그 배율 0.3 ≈ ×1.35로 명시해 고쳤다). 근거 수치·나머지 변경은 01 v0.26*
- *v0.29 — **영향성 탭 순차 재생**(§4). 사용자 지적 "지금은 그냥 다 빛나고 있잖아" — 원뿔이 통째로 한 번에 켜져서 **무엇이 무엇을 먼저 건드리는지**가 화면에 없었다(순서를 말하는 표현이 420 ms짜리 링 하나뿐이었다). 층 슬롯 + 층 안 시차로 한 줄씩 연결되게 하고 자동 반복한다. **시간축을 기하에서 분리한 것이 이 변경의 요지다** — 종전에는 `layout.ranks`를 그대로 파급 시각에 썼는데 전파 폭포의 ranks는 **모듈 밴드 열 인덱스**라 간선 방향 단조가 아니었다(링에서는 안 보였고 순차 재생을 붙이는 순간 간선이 거꾸로 자라는 것으로 드러났을 결함). 신설 `lib/influenceplay.js`가 `assignRanks`를 스스로 돌려 배치와 무관한 스케줄을 내고, 자막·요약·배치 설명이 **같은 층 수**를 쓴다(`graphDepth`) — 종전 배치 설명의 "16층"은 엔진 소유 수를 웹이 베껴 둔 §5.5 위반이었고 실제 화면은 21층(파라미터·입력 + IR 16 + 출력·기체·지표)이라 이번에 어긋남이 드러났다. 노드 점등은 **첫 도착 간선의 성장 완료**다(max면 선이 닿았는데도 어둡고, 랭크 시각이면 선이 자라는 중에 노드가 먼저 켜져 허공에 매달린 선이 된다). 부수: `struct` 캐시를 **선택 무관**으로 돌려 클릭 한 번마다 풀해상도 캔버스 두 장을 새로 할당하던 것(dpr 2에서 ~24 MB)과 매 프레임 원뿔 밖 간선 123개를 캐시 없이 다시 긋던 `drawContext`를 함께 제거, 레이어는 크기가 같으면 재사용(`ensureLayer`). 세 배치의 간선에 `idx`(모델 간선 인덱스)를 실어, 좌표 없는 간선이 `filter`로 빠지면 원뿔 판정이 조용히 어긋나던 자리를 막았다. `wavefrontSchedule`은 `conePlayback`에 흡수돼 삭제(씨앗=0·미도달=null·단조 세 성질은 테스트로 승계). `prefers-reduced-motion`이면 타이머가 없어 진행시킬 틱이 없으므로 **완료 프레임**(t = playMs)에 착지한다 — 정지 프레임은 최대치가 아니라 완료/평균이라는 기존 규칙 그대로*
